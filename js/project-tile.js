const tile = document.querySelector(".cf-proj-vidya");

let count = 1;

tile.onload = () => {
  const className = obj.constructor.name;
  buttonA.textContent = "Try again!";
  // headingA.textContent = `${count} clicks so far`;
  count += 1;
};